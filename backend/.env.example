DATABASE_URL=postgresql://timetowork:timetowork@localhost:5432/timetowork
JWT_SECRET=change-me-in-production
PORT=4000

APP_URL=http://localhost:5173

# DANGER — dev/local use only, see .env.example at the repo root
DEV_MODE=false

SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
